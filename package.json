{
  "name": "creer-inmobiliaria",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.4.5",
    "@twilio/runtime-handler": "1.1.2",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.2.1"
  },
  "engines": {
    "node": "12"
  }
}
